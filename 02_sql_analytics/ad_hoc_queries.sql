-- ad_hoc_queries.sql
-- Scratch space for exploratory / one-off queries during development.
-- Keep the ones worth remembering; delete the rest before submitting.

-- Example: row counts across all Bronze tables
-- SELECT 'sales_transactions' AS table_name, COUNT(*) FROM raw.sales_transactions
-- UNION ALL
-- SELECT 'customer_master', COUNT(*) FROM raw.customer_master;
